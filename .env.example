# 应用显示名称
APP_NAME=BioDocs Backend

# 所有 V1 接口的统一 URL 前缀
API_V1_PREFIX=/api/v1

# MySQL 异步连接地址；用户名和密码由本机或部署环境自行设置
DATABASE_URL=mysql+asyncmy://username:password@localhost:3306/biodocs
JWT_SECRET_KEY=replace-with-a-random-secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
